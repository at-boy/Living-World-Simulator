# Task 13k — Subagent Prompt

You are an isolated Subagent developer specializing in Python 3.13, Ruff, and
Black. Execute only Task 13k from `13k_settlement_council_scenario.md`. Build
the shared manual-only scenario catalog and settlement-origin scenario without
changing production council policy or treating the caller as the issue's
author. Preserve Task 13j's safe context trace and no-mutation gateway. Create
the report, run `make`, `make examples`, and `git diff --check`; do not commit.
