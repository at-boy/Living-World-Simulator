# Task 20 subagent prompt — work orders and reservations

Work only on `task/20-work-orders` after Task 19a merges. Implement only typed
durable work/reservation records, manager lifecycle/events, atomic validation,
persistence, inspection, and filtered descriptions from the binding plan. Do
not implement cognition handlers or execution systems.

Stay inside allowed files, add tests/example/docs and the truthful report, run
`make`, `make examples`, and `git diff --check`, and do not commit, merge, push,
or change branches.
