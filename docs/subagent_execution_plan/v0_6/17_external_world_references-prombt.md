# Task 17 subagent prompt — external-world references

Work only on `task/17-external-world-references`. Read required docs, Task
15b's report, and the binding plan. Implement only partial off-map anchors,
manager lifecycle/events, persistence migration, detached inspection, and the
explicit filtered NPC interpretation. Never invent a complete remote world or
expose hidden configuration, IDs, probabilities, or outcomes.

Stay inside allowed files, add tests/example/docs and the truthful report, and
run `make`, `make examples`, and `git diff --check`. Do not commit, merge, push,
or change branches; hand the uncommitted delivery to the orchestrator.
