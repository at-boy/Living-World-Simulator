# Task 15c subagent prompt — Spatial Inspector view

Work only on `task/15c-spatial-inspector-view` after Tasks 15 and 15b merge.
Read required docs and prerequisite reports. Render only the canonical detached
spatial DTO in the read-only inspector with documented states and click-through.
Do not change domain placement, add write requests, infer coordinates, or feed
map data into cognition.

Stay inside allowed files, add focused tests/docs and the truthful report, and
run `make`, `make examples`, and `git diff --check`. Do not commit, merge, push,
or change branches; hand the uncommitted delivery to the orchestrator.
