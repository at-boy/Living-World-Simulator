# Task 15 subagent prompt — FastAPI-hosted World Inspector

Work only on `task/15-world-inspector-ui` after its dependencies are reviewed
and merged. Read `AGENTS.md`, authoritative docs, prerequisite reports, and the
Task 15 plan. Implement the same-origin static HTML/CSS/JavaScript inspector
without Node tooling. Consume only detached read-only endpoints and implement
the documented views and failure states. Do not implement the spatial map,
administration, mutation routes, or any NPC/cognition integration.

Stay within the plan's allowed files; stop for a documented amendment if
needed. Add tests/docs and a truthful Task 15 report. Run `make`,
`make examples`, and `git diff --check`. Do not commit, merge, push, or change
branches; hand the uncommitted delivery to the orchestrator.
