# Task 20b subagent prompt — deterministic work execution

Work only on `task/20b-work-execution` after Task 20a merges. Implement the
stable assignment/progress/resource/domain-effect systems and immutable events
from the binding plan. Use managers and existing domain contracts; never let
LLM output execute effects or directly complete goals/stages.

Stay inside allowed files, add tests/example/docs and the truthful report, run
`make`, `make examples`, and `git diff --check`, and do not commit, merge, push,
or change branches.
