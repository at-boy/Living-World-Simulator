# Task 15b subagent prompt — spatial domain, persistence, and inspection

Work only on `task/15b-spatial-domain-persistence`. Read all required docs,
Task 15a's ADR/report, and the binding Task 15b plan. Implement only typed
placement records, manager ownership, persistence migration, deterministic
queries, and detached read-only inspection. Do not implement UI, pathfinding,
work execution, or NPC coordinate exposure.

Stay inside allowed files, add tests/example/docs and the truthful report, and
run `make`, `make examples`, and `git diff --check`. Do not commit, merge, push,
or change branches; hand the uncommitted delivery to the orchestrator.
