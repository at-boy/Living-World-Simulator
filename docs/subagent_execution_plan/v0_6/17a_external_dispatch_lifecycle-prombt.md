# Task 17a subagent prompt — external dispatch lifecycle

Work only on `task/17a-external-dispatch-lifecycle` after Task 17 merges. Read
required docs and prerequisite report. Implement only validated durable
dispatch lifecycle, deterministic system outcomes, manager-owned resources and
events, gateway handler, persistence, inspection, and safe perceptions. LLM
proposals never determine authoritative outcomes.

Stay inside allowed files, add tests/example/docs and the truthful report, and
run `make`, `make examples`, and `git diff --check`. Do not commit, merge, push,
or change branches; hand the uncommitted delivery to the orchestrator.
