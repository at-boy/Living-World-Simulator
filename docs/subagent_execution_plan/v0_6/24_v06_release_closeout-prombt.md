# Task 24 subagent prompt — v0.6 release-readiness closeout

Work only on `task/24-v06-closeout` after every prerequisite report/merge is
present. Audit rather than assume completion. Update only release/documentation
surfaces allowed by the binding plan; request isolated correction tasks for
implementation defects. Re-run all acceptance and validation commands and
produce the truthful closeout report.

Never merge, commit, tag, or push to `main`. Do not commit, merge, push, or
change branches; hand the uncommitted delivery to the orchestrator for review.
