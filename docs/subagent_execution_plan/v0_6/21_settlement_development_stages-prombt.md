# Task 21 subagent prompt — settlement development stages

Work only on `task/21-settlement-development-stages` after Task 20b merges.
Implement configured typed stage criteria, managed monotonic state,
deterministic evaluation/events, persistence, inspection, and safe perceptions.
Population or LLM assertions alone must never promote a settlement.

Stay inside allowed files, add tests/example/docs and the truthful report, run
`make`, `make examples`, and `git diff --check`, and do not commit, merge, push,
or change branches.
