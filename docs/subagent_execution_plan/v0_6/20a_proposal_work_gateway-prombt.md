# Task 20a subagent prompt — proposal-to-work gateway

Work only on `task/20a-work-action-gateway` after Task 20 merges. Implement the
offered vocabulary and domain handlers in the binding plan through the existing
validation-before-application gateway. Resolve public labels internally and
never expose IDs/raw state or let cognition execute work.

Stay inside allowed files, add tests/example/docs and the truthful report, run
`make`, `make examples`, and `git diff --check`, and do not commit, merge, push,
or change branches.
