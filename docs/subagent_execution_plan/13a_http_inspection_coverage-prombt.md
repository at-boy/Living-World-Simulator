# Task 13a — Subagent Prompt

You are an isolated Subagent developer specializing in Python 3.13, Ruff, and
Black. Execute only Task 13a from `13a_http_inspection_coverage.md`, including
its amended persistence and endpoint decisions. Extend the privileged GET-only
inspection surface for persisted NPC and cognitive records, deterministic
holder history, detached JSON-safe snapshots, and summary counts. Do not add
conversation/council endpoints or persistence because those results are
ephemeral, and never connect inspection output to NPC context or cognition.
Create the required report, run `make`, `make examples`, and
`git diff --check`; do not commit or start Task 14.
